import uuid

class generateUuid(object):

    def generate_uuid_type_four(self):
        '''This creates a keyword named "Generate Uuid Type Four"
        This keyword takes two argument. It returns the string back into number
        '''
        return uuid.uuid4()